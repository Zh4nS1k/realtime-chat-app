version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
      - ./client/.env.local
    environment:
      - CLIENT_ORIGIN=http://localhost:3000
    ports:
      - "4001:4001"
      - "3000:3000"
    restart: unless-stopped
